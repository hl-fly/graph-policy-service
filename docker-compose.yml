services:
  graph-policy-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: graph-policy-service
    ports:
      - "8080:8080"
    environment:
      - SERVER_ADDRESS=0.0.0.0:8080
    volumes:
      - ./.env:/app/.env:ro
    restart: unless-stopped

